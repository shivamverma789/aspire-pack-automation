<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Categories | Aspire Packaging Admin</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/aos@2.3.4/dist/aos.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/aos@2.3.4/dist/aos.css" rel="stylesheet">
    <style>
        :root {
            --primary: #8b0000;
            --primary-light: #a31a1a;
            --primary-dark: #660000;
            --text-primary: #2c3e50;
            --text-secondary: #6c757d;
            --text-light: #ffffff;
            --bg-primary: #ffffff;
            --bg-secondary: #f8f9fa;
            --bg-dark: #1a1a1a;
            --border: #dee2e6;
            --shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            --shadow-hover: 0 8px 30px rgba(0, 0, 0, 0.15);
            --gradient-primary: linear-gradient(135deg, var(--primary), var(--primary-light));
            --transition: all 0.3s ease;
            --primary-hover: #7a0000;

            /* Admin specific colors */
            --admin-sidebar-bg: #1a1a1a; /* Darker background for admin sidebar */
            --admin-sidebar-text: #ffffff;
            --admin-sidebar-hover: #2d2d2d;
            --admin-card-bg: #ffffff;
            --admin-card-border: #e1e5e9;
            --admin-card-shadow: 0 2px 10px rgba(0,0,0,0.08);
            --admin-card-hover-shadow: 0 8px 20px rgba(0,0,0,0.12);
        }
        [data-theme="dark"] {
            --text-primary: #ffffff;
            --text-secondary: #b8bcc8;
            --bg-primary: #1a1a1a;
            --bg-secondary: #2d2d2d;
            --border: #404040;
            --admin-sidebar-bg: #0d0d0d;
            --admin-sidebar-text: #e0e0e0;
            --admin-sidebar-hover: #1a1a1a;
            --admin-card-bg: #2d2d2d;
            --admin-card-border: #404040;
            --admin-card-shadow: 0 2px 10px rgba(0,0,0,0.2);
            --admin-card-hover-shadow: 0 8px 20px rgba(0,0,0,0.3);
        }
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: 'Inter', sans-serif;
            background: var(--bg-secondary);
            color: var(--text-primary);
            line-height: 1.6;
            display: flex;
            min-height: 100vh;
            overflow-x: hidden;
        }

        .preloader {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--bg-primary);
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 9999;
            transition: opacity 0.5s ease;
        }
        .preloader.hidden {
            opacity: 0;
            pointer-events: none;
        }
        .loader {
            width: 50px;
            height: 50px;
            border: 3px solid var(--border);
            border-top: 3px solid var(--primary);
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        /* Sidebar Styles (Copied from admin-dashboard.ejs) */
        .sidebar {
            width: 280px;
            background-color: var(--admin-sidebar-bg);
            color: var(--admin-sidebar-text);
            padding: 2rem 1.5rem;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            box-shadow: var(--shadow);
            position: sticky;
            top: 0;
            height: 100vh;
            transition: width 0.3s ease, background-color 0.3s ease;
            flex-shrink: 0;
        }
        .sidebar-header {
            margin-bottom: 2.5rem;
            text-align: center;
        }
        .sidebar-header h2 {
            font-family: 'Poppins', sans-serif;
            font-size: 1.8rem;
            font-weight: 700;
            color: var(--primary);
            margin-bottom: 0.5rem;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }
        .sidebar-header h2 i {
            font-size: 1.5rem;
        }
        .sidebar-nav {
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            gap: 0.75rem;
        }
        .sidebar-nav a, .sidebar-footer a {
            text-decoration: none;
            color: var(--admin-sidebar-text);
            padding: 0.8rem 1rem;
            display: flex;
            align-items: center;
            gap: 1rem;
            border-radius: 8px;
            transition: background-color 0.3s ease, color 0.3s ease, transform 0.2s ease;
            font-weight: 500;
            font-size: 1rem;
        }
        .sidebar-nav a i, .sidebar-footer a i {
            font-size: 1.1rem;
            width: 20px; /* Fixed width for icons */
            text-align: center;
            color: var(--primary-light); /* Icon color */
        }
        .sidebar-nav a:hover {
            background-color: var(--admin-sidebar-hover);
            transform: translateX(5px);
        }
        .sidebar-nav a.active {
            background-color: var(--primary);
            color: var(--text-light);
            box-shadow: var(--shadow);
        }
        .sidebar-nav a.active i {
            color: var(--text-light);
        }
        .sidebar-footer {
            margin-top: 2rem;
            padding-top: 1.5rem;
            border-top: 1px solid rgba(255,255,255,0.1);
        }
        .sidebar-footer a {
            justify-content: center;
            background-color: rgba(255,255,255,0.05);
            color: var(--text-light);
        }
        .sidebar-footer a:hover {
            background-color: var(--primary-dark);
            transform: translateY(-3px);
        }
        .sidebar-footer a i {
            color: var(--text-light);
        }

        /* Main Content Styles */
        .main-content {
            flex-grow: 1;
            padding: 2.5rem;
            background: var(--bg-secondary);
            transition: background-color 0.3s ease;
        }
        .main-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2.5rem;
            flex-wrap: wrap;
            gap: 1rem;
        }
        .main-header h1 {
            font-family: 'Poppins', sans-serif;
            font-size: 2.5rem;
            font-weight: 700;
            color: var(--primary);
        }
        .back-to-web-btn {
            background: var(--primary);
            color: white;
            padding: 0.75rem 1.5rem;
            border: none;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            transition: var(--transition);
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 8px;
            box-shadow: var(--shadow);
        }
        .back-to-web-btn:hover {
            background: var(--primary-hover);
            transform: translateY(-2px);
            box-shadow: var(--shadow-hover);
        }

        /* Category List Specific Styles */
        .add-category-btn {
            background: var(--gradient-primary);
            color: var(--text-light);
            padding: 0.75rem 1.5rem;
            border: none;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            transition: var(--transition);
            cursor: pointer;
            display: inline-flex; /* Use inline-flex for button */
            align-items: center;
            gap: 8px;
            box-shadow: var(--shadow);
            margin-bottom: 2rem; /* Spacing below the button */
        }
        .add-category-btn:hover {
            transform: translateY(-2px);
            box-shadow: var(--shadow-hover);
        }

        .category-block {
            background: var(--admin-card-bg);
            border: 1px solid var(--admin-card-border);
            padding: 1.5rem;
            border-radius: 12px;
            box-shadow: var(--admin-card-shadow);
            margin-bottom: 1.5rem;
            transition: all 0.3s ease;
        }
        .category-block:hover {
            box-shadow: var(--admin-card-hover-shadow);
        }

        .category-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 1rem;
            padding-bottom: 0.75rem;
            border-bottom: 1px solid var(--border);
            flex-wrap: wrap; /* Allow wrapping on smaller screens */
            gap: 0.75rem; /* Gap for wrapped items */
        }
        .category-info {
            display: flex;
            align-items: center;
            flex-grow: 1; /* Allow info to take available space */
        }
        .category-info img {
            height: 70px;
            width: 70px;
            object-fit: cover;
            border-radius: 8px;
            margin-right: 15px;
            border: 1px solid var(--border);
            flex-shrink: 0; /* Prevent image from shrinking */
        }
        .category-name {
            font-family: 'Poppins', sans-serif;
            font-size: 1.4rem;
            font-weight: 600;
            color: var(--text-primary);
            word-break: break-word; /* Break long names */
        }
        .category-actions {
            display: flex;
            gap: 0.5rem;
            flex-wrap: wrap; /* Allow actions to wrap */
            justify-content: flex-end; /* Align actions to the right */
        }
        .category-actions a, .subcategory-actions a {
            padding: 8px 12px;
            border-radius: 6px;
            text-decoration: none;
            font-size: 0.85rem;
            font-weight: 500;
            display: inline-flex;
            align-items: center;
            gap: 6px;
            transition: all 0.3s ease;
            white-space: nowrap; /* Prevent button text from wrapping */
        }
        .edit-btn { background-color: #007bff; color: white; }
        .edit-btn:hover { background-color: #0056b3; transform: translateY(-2px); }
        .delete-btn { background-color: #dc3545; color: white; }
        .delete-btn:hover { background-color: #a71d2a; transform: translateY(-2px); }
        .view-btn { background-color: #28a745; color: white; }
        .view-btn:hover { background-color: #1e7e34; transform: translateY(-2px); }

        .subcategories {
            margin-top: 1rem;
            padding-left: 2rem;
            border-left: 2px solid var(--border);
        }
        .subcategory {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 0.5rem;
            padding: 0.75rem 1rem;
            background: var(--bg-secondary);
            border-radius: 8px;
            border: 1px solid var(--border);
            flex-wrap: wrap;
            gap: 0.5rem;
        }
        .subcategory-name {
            font-weight: 500;
            color: var(--text-primary);
            font-size: 1rem;
            flex-grow: 1;
            word-break: break-word;
        }
        .subcategory-actions {
            display: flex;
            gap: 0.5rem;
            flex-wrap: wrap;
            justify-content: flex-end;
        }
        .no-categories-message {
            text-align: center;
            padding: 3rem;
            color: var(--text-secondary);
            font-size: 1.1rem;
            background: var(--admin-card-bg);
            border-radius: 12px;
            box-shadow: var(--admin-card-shadow);
            border: 1px solid var(--admin-card-border);
        }
        .no-categories-message i {
            font-size: 3rem;
            margin-bottom: 1rem;
            color: var(--primary);
            opacity: 0.7;
        }

        /* Responsive Adjustments */
        @media (max-width: 1024px) {
            .sidebar {
                width: 250px;
            }
            .main-content {
                padding: 2rem;
            }
            .main-header h1 {
                font-size: 2.2rem;
            }
        }
        @media (max-width: 768px) {
            body {
                flex-direction: column;
            }
            .sidebar {
                width: 100%;
                height: auto;
                position: static;
                padding: 1.5rem 1rem;
                box-shadow: var(--shadow);
            }
            .sidebar-header {
                margin-bottom: 1.5rem;
            }
            .sidebar-nav {
                flex-direction: row;
                flex-wrap: wrap;
                justify-content: center;
                gap: 0.5rem;
            }
            .sidebar-nav a {
                padding: 0.6rem 0.8rem;
                font-size: 0.9rem;
                gap: 0.5rem;
            }
            .sidebar-nav a i {
                font-size: 1rem;
                width: auto;
            }
            .sidebar-footer {
                margin-top: 1.5rem;
                padding-top: 1rem;
            }
            .main-content {
                padding: 1.5rem;
            }
            .main-header {
                flex-direction: column;
                align-items: flex-start;
                margin-bottom: 1.5rem;
            }
            .main-header h1 {
                font-size: 2rem;
                margin-bottom: 1rem;
            }
            .category-header {
                flex-direction: column;
                align-items: flex-start;
            }
            .category-actions {
                width: 100%;
                justify-content: flex-start;
            }
            .category-info img {
                height: 50px;
                width: 50px;
            }
            .category-name {
                font-size: 1.2rem;
            }
            .subcategory {
                flex-direction: column;
                align-items: flex-start;
            }
            .subcategory-actions {
                width: 100%;
                justify-content: flex-start;
            }
        }
        @media (max-width: 480px) {
            .sidebar-header h2 {
                font-size: 1.5rem;
            }
            .sidebar-nav a {
                font-size: 0.85rem;
                padding: 0.5rem 0.75rem;
            }
            .main-header h1 {
                font-size: 1.8rem;
            }
            .category-info img {
                margin-right: 10px;
            }
            .category-name {
                font-size: 1.1rem;
            }
            .category-actions a, .subcategory-actions a {
                padding: 6px 10px;
                font-size: 0.8rem;
            }
        }
    </style>
</head>
<body>
    <div class="preloader" id="preloader">
        <div class="loader"></div>
    </div>

    <aside class="sidebar" data-aos="fade-right" data-aos-duration="800">
        <div>
            <div class="sidebar-header">
                <h2><i class="fas fa-cogs"></i> Admin Panel</h2>
            </div>
            <nav class="sidebar-nav">
                <a href="/admin/dashboard"><i class="fas fa-tachometer-alt"></i> Dashboard</a>
                <a href="/admin/products"><i class="fas fa-box-open"></i> Manage Products</a>
                <a href="/admin/categories" class="active"><i class="fas fa-th-large"></i> Manage Categories</a>
                <a href="/admin/certificates"><i class="fas fa-certificate"></i> Manage Certificates</a>
                <a href="/admin/users"><i class="fas fa-users"></i> Manage Users</a>
                <a href="/admin/settings"><i class="fas fa-cog"></i> Settings</a>
            </nav>
        </div>
        <div class="sidebar-footer">
            <a href="/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
        </div>
    </aside>

    <main class="main-content">
        <div class="main-header" data-aos="fade-down" data-aos-duration="800" data-aos-delay="200">
            <h1>Manage Categories</h1>
            <a href="/" class="back-to-web-btn">
                <i class="fas fa-globe"></i> Back to User Web
            </a>
        </div>

        <a href="/admin/categories/create" class="add-category-btn" data-aos="fade-up" data-aos-delay="300">
            <i class="fas fa-plus"></i> Create New Category
        </a>

        <% if (typeof categories !== 'undefined' && categories && Array.isArray(categories) && categories.length > 0) { %>
            <% categories.forEach((cat, index) => { %>
                <div class="category-block" data-aos="fade-up" data-aos-delay="<%= 400 + (index * 100) %>">
                    <div class="category-header">
                        <div class="category-info">
                            <img src="<%= cat.coverImage || '/placeholder.svg?height=70&width=70&text=Category' %>" alt="<%= cat.name %> Cover" />
                            <div class="category-name"><%= cat.name %></div>
                        </div>
                        <div class="category-actions">
                            <a href="/admin/categories/edit/<%= cat._id %>" class="edit-btn" aria-label="Edit <%= cat.name %>">
                                <i class="fas fa-edit"></i> Edit
                            </a>
                            <a href="/admin/categories/delete/<%= cat._id %>" onclick="return confirm('Are you sure you want to delete <%= cat.name %> and all its subcategories? This action cannot be undone.');" class="delete-btn" aria-label="Delete <%= cat.name %>">
                                <i class="fas fa-trash-alt"></i> Delete
                            </a>
                            <a href="/category/<%= cat.slug %>" target="_blank" class="view-btn" aria-label="View <%= cat.name %> on website">
                                <i class="fas fa-eye"></i> View
                            </a>
                        </div>
                    </div>
                    <% if (cat.subcategories && cat.subcategories.length > 0) { %>
                        <div class="subcategories">
                            <% cat.subcategories.forEach(sub => { %>
                                <div class="subcategory">
                                    <div class="subcategory-name">↳ <%= sub.name %></div>
                                    <div class="subcategory-actions">
                                        <a href="/admin/subcategories/edit/<%= sub._id %>" class="edit-btn" aria-label="Edit subcategory <%= sub.name %>">
                                            <i class="fas fa-edit"></i> Edit
                                        </a>
                                        <a href="/admin/subcategories/delete/<%= sub._id %>" onclick="return confirm('Are you sure you want to delete subcategory <%= sub.name %>? This action cannot be undone.');" class="delete-btn" aria-label="Delete subcategory <%= sub.name %>">
                                            <i class="fas fa-trash-alt"></i> Delete
                                        </a>
                                        <a href="/category/<%= sub.slug %>" target="_blank" class="view-btn" aria-label="View subcategory <%= sub.name %> on website">
                                            <i class="fas fa-eye"></i> View
                                        </a>
                                    </div>
                                </div>
                            <% }) %>
                        </div>
                    <% } %>
                </div>
            <% }) %>
        <% } else { %>
            <div class="no-categories-message" data-aos="fade-up" data-aos-delay="400">
                <i class="fas fa-th-large"></i>
                <h3>No Categories Found</h3>
                <p>It looks like there are no categories to display. Start by creating a new one!</p>
            </div>
        <% } %>
    </main>

    <script>
        window.addEventListener('load', function() {
            const preloader = document.getElementById('preloader');
            setTimeout(() => {
                preloader.classList.add('hidden');
            }, 500);
        });
        AOS.init({
            duration: 1000,
            once: true,
            offset: 50
        });
    </script>
</body>
</html>
