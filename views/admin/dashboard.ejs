<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard | Aspire Packaging</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/aos@2.3.4/dist/aos.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/aos@2.3.4/dist/aos.css" rel="stylesheet">
    <style>
        :root {
            --primary: #8b0000;
            --primary-light: #a31a1a;
            --primary-dark: #660000;
            --text-primary: #2c3e50;
            --text-secondary: #6c757d;
            --text-light: #ffffff;
            --bg-primary: #ffffff;
            --bg-secondary: #f8f9fa;
            --bg-dark: #1a1a1a;
            --border: #dee2e6;
            --shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            --shadow-hover: 0 8px 30px rgba(0, 0, 0, 0.15);
            --gradient-primary: linear-gradient(135deg, var(--primary), var(--primary-light));
            --transition: all 0.3s ease;
            --primary-hover: #7a0000;

            /* Admin specific colors */
            --admin-sidebar-bg: #1a1a1a; /* Darker background for admin sidebar */
            --admin-sidebar-text: #ffffff;
            --admin-sidebar-hover: #2d2d2d;
            --admin-card-bg: #ffffff;
            --admin-card-border: #e1e5e9;
            --admin-card-shadow: 0 2px 10px rgba(0,0,0,0.08);
            --admin-card-hover-shadow: 0 8px 20px rgba(0,0,0,0.12);
        }
        [data-theme="dark"] {
            --text-primary: #ffffff;
            --text-secondary: #b8bcc8;
            --bg-primary: #1a1a1a;
            --bg-secondary: #2d2d2d;
            --border: #404040;
            --admin-sidebar-bg: #0d0d0d;
            --admin-sidebar-text: #e0e0e0;
            --admin-sidebar-hover: #1a1a1a;
            --admin-card-bg: #2d2d2d;
            --admin-card-border: #404040;
            --admin-card-shadow: 0 2px 10px rgba(0,0,0,0.2);
            --admin-card-hover-shadow: 0 8px 20px rgba(0,0,0,0.3);
        }
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: 'Inter', sans-serif;
            background: var(--bg-secondary);
            color: var(--text-primary);
            line-height: 1.6;
            display: flex;
            min-height: 100vh;
            overflow-x: hidden;
        }

        .preloader {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--bg-primary);
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 9999;
            transition: opacity 0.5s ease;
        }
        .preloader.hidden {
            opacity: 0;
            pointer-events: none;
        }
        .loader {
            width: 50px;
            height: 50px;
            border: 3px solid var(--border);
            border-top: 3px solid var(--primary);
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        /* Sidebar Styles */
        .sidebar {
            width: 280px;
            background-color: var(--admin-sidebar-bg);
            color: var(--admin-sidebar-text);
            padding: 2rem 1.5rem;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            box-shadow: var(--shadow);
            position: sticky;
            top: 0;
            height: 100vh;
            transition: width 0.3s ease, background-color 0.3s ease;
            flex-shrink: 0;
        }
        .sidebar-header {
            margin-bottom: 2.5rem;
            text-align: center;
        }
        .sidebar-header h2 {
            font-family: 'Poppins', sans-serif;
            font-size: 1.8rem;
            font-weight: 700;
            color: var(--primary);
            margin-bottom: 0.5rem;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }
        .sidebar-header h2 i {
            font-size: 1.5rem;
        }
        .sidebar-nav {
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            gap: 0.75rem;
        }
        .sidebar-nav a, .sidebar-footer a {
            text-decoration: none;
            color: var(--admin-sidebar-text);
            padding: 0.8rem 1rem;
            display: flex;
            align-items: center;
            gap: 1rem;
            border-radius: 8px;
            transition: background-color 0.3s ease, color 0.3s ease, transform 0.2s ease;
            font-weight: 500;
            font-size: 1rem;
        }
        .sidebar-nav a i, .sidebar-footer a i {
            font-size: 1.1rem;
            width: 20px; /* Fixed width for icons */
            text-align: center;
            color: var(--primary-light); /* Icon color */
        }
        .sidebar-nav a:hover {
            background-color: var(--admin-sidebar-hover);
            transform: translateX(5px);
        }
        .sidebar-nav a.active {
            background-color: var(--primary);
            color: var(--text-light);
            box-shadow: var(--shadow);
        }
        .sidebar-nav a.active i {
            color: var(--text-light);
        }
        .sidebar-footer {
            margin-top: 2rem;
            padding-top: 1.5rem;
            border-top: 1px solid rgba(255,255,255,0.1);
        }
        .sidebar-footer a {
            justify-content: center;
            background-color: rgba(255,255,255,0.05);
            color: var(--text-light);
        }
        .sidebar-footer a:hover {
            background-color: var(--primary-dark);
            transform: translateY(-3px);
        }
        .sidebar-footer a i {
            color: var(--text-light);
        }

        /* Main Content Styles */
        .main-content {
            flex-grow: 1;
            padding: 2.5rem;
            background: var(--bg-secondary);
            transition: background-color 0.3s ease;
        }
        .main-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2.5rem;
            flex-wrap: wrap;
            gap: 1rem;
        }
        .main-header h1 {
            font-family: 'Poppins', sans-serif;
            font-size: 2.5rem;
            font-weight: 700;
            color: var(--primary);
        }
        .back-to-web-btn {
            background: var(--primary);
            color: white;
            padding: 0.75rem 1.5rem;
            border: none;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            transition: var(--transition);
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 8px;
            box-shadow: var(--shadow);
        }
        .back-to-web-btn:hover {
            background: var(--primary-hover);
            transform: translateY(-2px);
            box-shadow: var(--shadow-hover);
        }

        /* Stats Grid */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2rem;
            margin-bottom: 3rem;
        }
        .stat-card {
            background: var(--admin-card-bg);
            border-radius: 12px;
            padding: 2rem;
            box-shadow: var(--admin-card-shadow);
            text-align: center;
            transition: var(--transition);
            border: 1px solid var(--admin-card-border);
            position: relative;
            overflow: hidden;
        }
        .stat-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 5px;
            background: var(--gradient-primary);
            border-radius: 12px 12px 0 0;
        }
        .stat-card:hover {
            transform: translateY(-8px);
            box-shadow: var(--admin-card-hover-shadow);
        }
        .stat-card h3 {
            font-family: 'Poppins', sans-serif;
            font-size: 1.3rem;
            font-weight: 600;
            margin-bottom: 1rem;
            color: var(--text-primary);
        }
        .stat-card p {
            font-size: 3.5rem;
            font-weight: 700;
            color: var(--primary);
            line-height: 1;
        }
        .stat-card .stat-icon {
            font-size: 2.5rem;
            color: var(--primary-light);
            margin-bottom: 1rem;
        }

        /* Recent Activity / Quick Actions */
        .dashboard-sections {
            display: grid;
            grid-template-columns: 1fr; /* Default to single column */
            gap: 2rem;
        }
        .section-card {
            background: var(--admin-card-bg);
            border-radius: 12px;
            padding: 2rem;
            box-shadow: var(--admin-card-shadow);
            border: 1px solid var(--admin-card-border);
        }
        .section-card h3 {
            font-family: 'Poppins', sans-serif;
            font-size: 1.5rem;
            font-weight: 600;
            color: var(--text-primary);
            margin-bottom: 1.5rem;
            border-bottom: 1px solid var(--border);
            padding-bottom: 0.75rem;
        }
        .activity-list {
            list-style: none;
            padding: 0;
        }
        .activity-item {
            display: flex;
            align-items: center;
            gap: 1rem;
            padding: 0.75rem 0;
            border-bottom: 1px dashed var(--border);
            color: var(--text-secondary);
            font-size: 0.95rem;
        }
        .activity-item:last-child {
            border-bottom: none;
        }
        .activity-item i {
            color: var(--primary);
            font-size: 1.1rem;
        }
        .quick-actions-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
            gap: 1rem;
        }
        .action-btn {
            background: var(--bg-secondary);
            border: 1px solid var(--border);
            border-radius: 8px;
            padding: 1.5rem 1rem;
            text-decoration: none;
            color: var(--text-primary);
            font-weight: 600;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 0.75rem;
            transition: var(--transition);
            text-align: center;
        }
        .action-btn i {
            font-size: 2rem;
            color: var(--primary);
        }
        .action-btn:hover {
            background: var(--primary);
            color: white;
            transform: translateY(-5px);
            box-shadow: var(--shadow);
        }
        .action-btn:hover i {
            color: white;
        }

        /* Responsive Adjustments */
        @media (min-width: 1024px) {
            .dashboard-sections {
                grid-template-columns: 2fr 1fr; /* Two columns for larger screens */
            }
        }
        @media (max-width: 768px) {
            body {
                flex-direction: column;
            }
            .sidebar {
                width: 100%;
                height: auto;
                position: static;
                padding: 1.5rem 1rem;
                box-shadow: var(--shadow);
            }
            .sidebar-header {
                margin-bottom: 1.5rem;
            }
            .sidebar-nav {
                flex-direction: row;
                flex-wrap: wrap;
                justify-content: center;
                gap: 0.5rem;
            }
            .sidebar-nav a {
                padding: 0.6rem 0.8rem;
                font-size: 0.9rem;
                gap: 0.5rem;
            }
            .sidebar-nav a i {
                font-size: 1rem;
                width: auto;
            }
            .sidebar-footer {
                margin-top: 1.5rem;
                padding-top: 1rem;
            }
            .main-content {
                padding: 1.5rem;
            }
            .main-header {
                flex-direction: column;
                align-items: flex-start;
                margin-bottom: 1.5rem;
            }
            .main-header h1 {
                font-size: 2rem;
                margin-bottom: 1rem;
            }
            .stats-grid {
                grid-template-columns: 1fr;
                gap: 1.5rem;
            }
            .stat-card p {
                font-size: 3rem;
            }
            .quick-actions-grid {
                grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            }
        }
        @media (max-width: 480px) {
            .sidebar-header h2 {
                font-size: 1.5rem;
            }
            .sidebar-nav a {
                font-size: 0.85rem;
                padding: 0.5rem 0.75rem;
            }
            .main-header h1 {
                font-size: 1.8rem;
            }
            .stat-card p {
                font-size: 2.8rem;
            }
            .stat-card h3 {
                font-size: 1.1rem;
            }
            .section-card h3 {
                font-size: 1.3rem;
            }
            .action-btn {
                padding: 1rem 0.5rem;
                font-size: 0.9rem;
            }
            .action-btn i {
                font-size: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <div class="preloader" id="preloader">
        <div class="loader"></div>
    </div>

    <aside class="sidebar" data-aos="fade-right" data-aos-duration="800">
        <div>
            <div class="sidebar-header">
                <h2><i class="fas fa-cogs"></i> Admin Panel</h2>
            </div>
            <nav class="sidebar-nav">
                <a href="/admin/dashboard" class="active"><i class="fas fa-tachometer-alt"></i> Dashboard</a>
                <a href="/admin/products"><i class="fas fa-box-open"></i> Manage Products</a>
                <a href="/admin/categories"><i class="fas fa-th-large"></i> Manage Categories</a>
                <a href="/admin/certificates"><i class="fas fa-certificate"></i> Manage Certificates</a>
                <a href="/admin/users"><i class="fas fa-users"></i> Manage Users</a>
                <a href="/admin/settings"><i class="fas fa-cog"></i> Settings</a>
            </nav>
        </div>
        <div class="sidebar-footer">
            <a href="/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
        </div>
    </aside>

    <main class="main-content">
        <div class="main-header" data-aos="fade-down" data-aos-duration="800" data-aos-delay="200">
            <h1>Dashboard Overview</h1>
            <a href="/" class="back-to-web-btn">
                <i class="fas fa-globe"></i> Back to User Web
            </a>
        </div>

        <div class="stats-grid">
            <div class="stat-card" data-aos="fade-up" data-aos-delay="300">
                <i class="fas fa-box-open stat-icon"></i>
                <h3>Total Products</h3>
                <p><%= typeof totalProducts !== 'undefined' ? totalProducts : 'N/A' %></p>
            </div>
            <div class="stat-card" data-aos="fade-up" data-aos-delay="400">
                <i class="fas fa-th-large stat-icon"></i>
                <h3>Total Categories</h3>
                <p><%= typeof totalCategories !== 'undefined' ? totalCategories : 'N/A' %></p>
            </div>
            <div class="stat-card" data-aos="fade-up" data-aos-delay="500">
                <i class="fas fa-certificate stat-icon"></i>
                <h3>Total Certificates</h3>
                <p><%= typeof totalCertificates !== 'undefined' ? totalCertificates : 'N/A' %></p>
            </div>
            <div class="stat-card" data-aos="fade-up" data-aos-delay="600">
                <i class="fas fa-users stat-icon"></i>
                <h3>Total Users</h3>
                <p><%= typeof totalUsers !== 'undefined' ? totalUsers : 'N/A' %></p>
            </div>
        </div>

        <div class="dashboard-sections">
            <div class="section-card" data-aos="fade-up" data-aos-delay="700">
                <h3>Recent Activity</h3>
                <ul class="activity-list">
                    <li class="activity-item"><i class="fas fa-plus-circle"></i> New product "Vacuum Sealer Pro" added by Admin. <small>5 mins ago</small></li>
                    <li class="activity-item"><i class="fas fa-edit"></i> Category "Filling Machines" updated. <small>1 hour ago</small></li>
                    <li class="activity-item"><i class="fas fa-user-plus"></i> New user registered: John Doe. <small>3 hours ago</small></li>
                    <li class="activity-item"><i class="fas fa-trash-alt"></i> Certificate "ISO 9001" removed. <small>Yesterday</small></li>
                    <li class="activity-item"><i class="fas fa-chart-line"></i> Dashboard data refreshed. <small>2 days ago</small></li>
                </ul>
            </div>

            <div class="section-card" data-aos="fade-up" data-aos-delay="800">
                <h3>Quick Actions</h3>
                <div class="quick-actions-grid">
                    <a href="/admin/products/new" class="action-btn">
                        <i class="fas fa-plus"></i> Add Product
                    </a>
                    <a href="/admin/categories/create" class="action-btn">
                        <i class="fas fa-folder-plus"></i> Add Category
                    </a>
                    <a href="/admin/register/123" class="action-btn">
                        <i class="fas fa-user-plus"></i> Add Admin
                    </a>
                    <a href="/admin/reports" class="action-btn">
                        <i class="fas fa-chart-bar"></i> View Reports
                    </a>
                </div>
            </div>
        </div>
    </main>

    <script>
        window.addEventListener('load', function() {
            const preloader = document.getElementById('preloader');
            setTimeout(() => {
                preloader.classList.add('hidden');
            }, 500);
        });
        AOS.init({
            duration: 1000,
            once: true,
            offset: 50
        });

        // Basic theme toggle (if you want it on admin panel too)
        // const themeToggle = document.getElementById('themeToggle'); // You'd need to add this button
        // const body = document.body;
        // const currentTheme = localStorage.getItem('theme') || 'light';
        // body.setAttribute('data-theme', currentTheme);
        // if (themeToggle) {
        //     themeToggle.addEventListener('click', function() {
        //         const newTheme = body.getAttribute('data-theme') === 'dark' ? 'light' : 'dark';
        //         body.setAttribute('data-theme', newTheme);
        //         localStorage.setItem('theme', newTheme);
        //     });
        // }
    </script>
</body>
</html>
