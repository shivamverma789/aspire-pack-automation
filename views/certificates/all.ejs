<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Certificates | Aspire Packaging</title>
  <style>
    body {
      font-family: "Segoe UI", sans-serif;
      background-color: #f9f9f9;
      margin: 0;
      padding: 0;
    }

    header {
      background-color: #8b0000;
      color: #fff;
      padding: 1rem 2rem;
      text-align: center;
    }

    h1 {
      margin: 0;
      font-size: 2rem;
    }

    .container {
      max-width: 1100px;
      margin: 2rem auto;
      padding: 0 1rem;
    }

    .certificates-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
      gap: 1.5rem;
    }

    .card {
      background-color: #fff;
      padding: 1rem;
      border: 1px solid #ddd;
      border-radius: 8px;
      transition: box-shadow 0.3s ease;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .card:hover {
      box-shadow: 0 0 10px rgba(139, 0, 0, 0.2);
    }

    .card img,
    .card iframe {
      width: 100%;
      height: 250px;
      object-fit: contain;
      border-radius: 4px;
      margin-bottom: 0.8rem;
      border: 1px solid #ccc;
    }

    .card h3 {
      font-size: 1.1rem;
      margin: 0.5rem 0;
      color: #8b0000;
      text-align: center;
    }

    .download-btn {
      margin-top: auto;
      padding: 0.5rem 1rem;
      background-color: #8b0000;
      color: white;
      text-decoration: none;
      text-align: center;
      border-radius: 4px;
      transition: background-color 0.3s ease;
    }

    .download-btn:hover {
      background-color: #a80000;
    }

    .no-certificates {
      text-align: center;
      color: #666;
      margin-top: 3rem;
    }
  </style>
</head>
<body>

  <header>
    <h1>Our Certificates</h1>
  </header>

  <div class="container">
    <% if (certificates.length === 0) { %>
      <p class="no-certificates">No certificates available at the moment.</p>
    <% } else { %>
      <div class="certificates-grid">
        <% certificates.forEach(certificate => { 
          const fileExtension = certificate.certificate.split('.').pop().toLowerCase();
          const isPDF = fileExtension === 'pdf';
        %>
          <div class="card">
            <% if (isPDF) { %>
              <iframe src="<%= certificate.certificate %>" frameborder="0"></iframe>
            <% } else { %>
              <img src="<%= certificate.certificate %>" alt="Certificate Preview">
            <% } %>
            <h3><%= certificate.name %></h3>
            <a href="<%= certificate.certificate %>" target="_blank" class="download-btn">📄 View / Download</a>
          </div>
        <% }) %>
      </div>
    <% } %>
  </div>

</body>
</html>
