<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Aspire Packaging | Industrial Packaging Solutions</title>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/aos@2.3.4/dist/aos.js"></script>
<link href="https://cdn.jsdelivr.net/npm/aos@2.3.4/dist/aos.css" rel="stylesheet">

<style>
    :root {
        --primary: #8b0000;
        --primary-light: #a31a1a;
        --primary-dark: #660000;
        --text-primary: #2c3e50;
        --text-secondary: #6c757d;
        --text-light: #ffffff;
        --bg-primary: #ffffff;
        --bg-secondary: #f8f9fa;
        --bg-dark: #1a1a1a;
        --border: #dee2e6;
        --shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        --shadow-hover: 0 8px 30px rgba(0, 0, 0, 0.15);
        --gradient-primary: linear-gradient(135deg, var(--primary), var(--primary-light));
        --success: #28a745;
        --warning: #ffc107;
        --info: #17a2b8;
        --transition: all 0.3s ease;
        --primary-hover: #7a0000;
    }
    [data-theme="dark"] {
        --text-primary: #ffffff;
        --text-secondary: #b8bcc8;
        --bg-primary: #1a1a1a;
        --bg-secondary: #2d2d2d;
        --border: #404040;
    }
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }
    body {
        font-family: 'Inter', sans-serif;
        background: var(--bg-primary);
        color: var(--text-primary);
        line-height: 1.6;
        overflow-x: hidden;
    }
    .preloader {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: var(--bg-primary);
        display: flex;
        justify-content: center;
        align-items: center;
        z-index: 9999;
        transition: opacity 0.5s ease;
    }
    .preloader.hidden {
        opacity: 0;
        pointer-events: none;
    }
    .loader {
        width: 50px;
        height: 50px;
        border: 3px solid var(--border);
        border-top: 3px solid var(--primary);
        border-radius: 50%;
        animation: spin 1s linear infinite;
    }
    @keyframes spin {
        0% { transform: rotate(0deg); }
        100% { transform: rotate(360deg); }
    }
    .navbar {
        background: var(--bg-primary);
        backdrop-filter: blur(10px);
        border-bottom: 1px solid var(--border);
        position: sticky;
        top: 0;
        z-index: 1000;
        padding: 1rem 0;
        transition: all 0.3s ease;
    }
    .navbar.scrolled {
        box-shadow: var(--shadow);
    }
    .nav-container {
        max-width: 1400px;
        margin: 0 auto;
        padding: 0 2rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }
    .logo-container {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        text-decoration: none;
        color: var(--primary);
        font-weight: 700;
        font-size: 1.5rem;
    }
    .logo-icon {
        width: 40px;
        height: 40px;
        background: var(--gradient-primary);
        border-radius: 8px;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
    }
    .nav-menu {
        display: flex;
        list-style: none;
        gap: 2.5rem;
        align-items: center;
    }
    .nav-link {
        color: var(--text-primary);
        text-decoration: none;
        font-weight: 500;
        position: relative;
        transition: color 0.3s ease;
    }
    .nav-link:hover {
        color: var(--primary);
    }
    .nav-link::after {
        content: '';
        position: absolute;
        bottom: -5px;
        left: 0;
        width: 0;
        height: 2px;
        background: var(--primary);
        transition: width 0.3s ease;
    }
    .nav-link:hover::after {
        width: 100%;
    }
    .nav-actions {
        display: flex;
        align-items: center;
        gap: 1rem;
    }
    .theme-toggle {
        background: var(--bg-secondary);
        border: 1px solid var(--border);
        color: var(--text-secondary);
        width: 40px;
        height: 40px;
        border-radius: 8px;
        cursor: pointer;
        transition: all 0.3s ease;
    }
    .theme-toggle:hover {
        background: var(--primary);
        color: white;
    }
    .btn-primary {
        background: var(--gradient-primary);
        color: var(--text-light);
        padding: 0.75rem 1.5rem;
        border: none;
        border-radius: 8px;
        text-decoration: none;
        font-weight: 600;
        transition: all 0.3s ease;
    }
    .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: var(--shadow-hover);
    }
    .mobile-toggle {
        display: none;
        background: none;
        border: none;
        font-size: 1.5rem;
        cursor: pointer;
        color: var(--text-primary);
    }
    .hero {
        background: linear-gradient(135deg, rgba(139, 0, 0, 0.9), rgba(139, 0, 0, 0.7)), 
                    url('https://images.unsplash.com/photo-1565793298595-6a879b1d9492?ixlib=rb-4.0.3&auto=format&fit=crop&w=2000&q=80') center/cover;
        color: var(--text-light);
        padding: 8rem 2rem;
        text-align: center;
        position: relative;
        overflow: hidden;
    }
    .hero::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grain" width="100" height="100" patternUnits="userSpaceOnUse"><circle cx="25" cy="25" r="1" fill="white" opacity="0.1"/><circle cx="75" cy="75" r="1" fill="white" opacity="0.1"/></pattern></defs><rect width="100" height="100" fill="url(%23grain)"/></svg>');
        opacity: 0.3;
    }
    .hero-content {
        position: relative;
        z-index: 2;
        max-width: 800px;
        margin: 0 auto;
    }
    .hero h1 {
        font-size: 3.5rem;
        font-weight: 700;
        margin-bottom: 1.5rem;
        line-height: 1.2;
    }
    .hero p {
        font-size: 1.3rem;
        margin-bottom: 2.5rem;
        opacity: 0.95;
    }
    .hero-buttons {
        display: flex;
        gap: 1rem;
        justify-content: center;
        flex-wrap: wrap;
    }
    .btn-hero {
        padding: 1rem 2rem;
        font-size: 1.1rem;
        border-radius: 50px;
        transition: all 0.3s ease;
    }
    .btn-hero.secondary {
        background: rgba(255, 255, 255, 0.2);
        backdrop-filter: blur(10px);
        border: 2px solid rgba(255, 255, 255, 0.3);
    }
    .stats {
        background: var(--primary);
        color: var(--text-light);
        padding: 4rem 2rem;
    }
    .stats-container {
        max-width: 1200px;
        margin: 0 auto;
    }
    .stats-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 3rem;
        text-align: center;
    }
    .stat-item {
        position: relative;
    }
    .stat-item::before {
        content: '';
        position: absolute;
        top: -20px;
        left: 50%;
        transform: translateX(-50%);
        width: 40px;
        height: 4px;
        background: rgba(255, 255, 255, 0.3);
        border-radius: 2px;
    }
    .stat-number {
        font-size: 3rem;
        font-weight: 700;
        margin-bottom: 0.5rem;
        background: linear-gradient(45deg, #fff, #f0f0f0);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }
    .stat-label {
        font-size: 1.1rem;
        opacity: 0.9;
    }
    .section {
        padding: 5rem 2rem;
    }
    .container {
        max-width: 1400px;
        margin: 0 auto;
    }
    .section-header {
        text-align: center;
        margin-bottom: 4rem;
    }
    .section-title {
        font-size: 2.5rem;
        font-weight: 700;
        color: var(--primary);
        margin-bottom: 1rem;
    }
    .section-subtitle {
        font-size: 1.2rem;
        color: var(--text-secondary);
        max-width: 600px;
        margin: 0 auto;
    }
    .categories-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 1.5rem;
    }
    .category-card {
        background: var(--bg-primary);
        border-radius: 20px;
        overflow: hidden;
        box-shadow: var(--shadow);
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        position: relative;
        border: 1px solid var(--border);
    }
    .category-card:hover {
        transform: translateY(-10px) scale(1.02);
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
        border-color: var(--primary-light);
    }
    .category-image {
        width: 100%;
        height: 180px;
        object-fit: cover;
        transition: transform 0.4s ease;
    }
    .category-card:hover .category-image {
        transform: scale(1.1);
    }
    .category-content {
        padding: 1.5rem;
        position: relative;
    }
    .category-title {
        font-size: 1.25rem;
        font-weight: 600;
        margin-bottom: 0.75rem;
        color: var(--text-primary);
        line-height: 1.3;
    }
    .category-description {
        color: var(--text-secondary);
        margin-bottom: 1rem;
        line-height: 1.5;
        font-size: 0.95rem;
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }
    .category-meta {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 1.25rem;
        font-size: 0.9rem;
    }
    .product-count {
        background: linear-gradient(135deg, var(--primary), var(--primary-light));
        color: white;
        padding: 0.4rem 0.8rem;
        border-radius: 15px;
        font-weight: 500;
        font-size: 0.85rem;
    }
    .category-arrow {
        color: var(--primary);
        font-size: 1.2rem;
        transition: transform 0.3s ease;
    }
    .category-card:hover .category-arrow {
        transform: translateX(5px);
    }
    .category-btn {
        width: 100%;
        padding: 0.8rem;
        background: transparent;
        color: var(--primary);
        border: 2px solid var(--primary);
        border-radius: 10px;
        text-decoration: none;
        display: inline-block;
        text-align: center;
        font-weight: 600;
        font-size: 0.95rem;
        transition: all 0.3s ease;
    }
    .category-btn:hover {
        background: var(--primary);
        color: white;
        transform: translateY(-2px);
    }
    .products-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); /* Adjusted for smaller cards */
        gap: 1.5rem; /* Adjusted gap */
    }
    .product-card {
        background: var(--bg-primary);
        border-radius: 12px; /* Adjusted border-radius */
        overflow: hidden;
        box-shadow: var(--shadow);
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        position: relative;
        border: 1px solid var(--border);
        cursor: pointer;
    }
    .product-card:hover {
        transform: translateY(-8px);
        box-shadow: 0 25px 50px rgba(0, 0, 0, 0.15);
        border-color: var(--primary-light);
    }
    .product-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 4px;
        background: var(--gradient-primary);
        opacity: 0;
        transition: opacity 0.3s ease;
    }
    .product-card:hover::before {
        opacity: 1;
    }
    .product-image-container {
        position: relative;
        overflow: hidden;
        height: 220px; /* Adjusted height */
    }
    .product-image {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: transform 0.4s ease;
    }
    .product-card:hover .product-image {
        transform: scale(1.05);
    }
    .product-badge {
        position: absolute;
        top: 12px; /* Adjusted position */
        left: 12px; /* Adjusted position */
        background: var(--primary); /* Changed to primary color */
        color: white;
        padding: 4px 8px; /* Adjusted padding */
        border-radius: 4px; /* Adjusted border-radius */
        font-size: 12px; /* Adjusted font-size */
        font-weight: 500; /* Adjusted font-weight */
    }
    .quick-view-btn {
        position: absolute;
        top: 12px; /* Adjusted position */
        right: 12px; /* Adjusted position */
        background: rgba(255, 255, 255, 0.9); /* Adjusted background */
        border: none;
        border-radius: 50%;
        width: 36px; /* Adjusted size */
        height: 36px; /* Adjusted size */
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1rem;
        color: var(--primary); /* Adjusted color */
        cursor: pointer;
        transition: var(--transition);
        opacity: 0;
        transform: scale(0.8); /* Added transform for animation */
    }
    .product-card:hover .quick-view-btn {
        opacity: 1;
        transform: scale(1); /* Added transform for animation */
    }
    .quick-view-btn:hover {
        background: white; /* Adjusted background */
        box-shadow: var(--shadow); /* Added shadow on hover */
    }
    .product-info {
        padding: 1.5rem; /* Adjusted padding */
    }
    .product-name {
        font-size: 1.2rem; /* Adjusted font-size */
        font-weight: 600;
        margin-bottom: 0.5rem;
        color: var(--text-primary);
        line-height: 1.4; /* Adjusted line-height */
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }
    .product-model {
        color: var(--text-secondary);
        margin-bottom: 1rem;
        font-size: 13px; /* Adjusted font-size */
        font-weight: 500; /* Adjusted font-weight */
    }
    .product-features {
        margin-bottom: 1.5rem;
    }
    .feature-item {
        display: flex;
        align-items: center;
        gap: 8px; /* Adjusted gap */
        color: var(--text-secondary);
        font-size: 13px; /* Adjusted font-size */
        margin-bottom: 4px; /* Adjusted margin */
    }
    .feature-icon {
        color: var(--primary);
        font-size: 12px; /* Adjusted font-size */
    }
    .product-actions {
        display: flex;
        gap: 0.75rem; 
    }
    .inquire-btn {
        width: 100%;
        padding: 12px;
        background: var(--primary);
        color: white;
        border: none;
        border-radius: 8px;
        font-weight: 500;
        cursor: pointer;
        transition: var(--transition);
        font-size: 14px;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 8px;
    }
    .inquire-btn:hover {
        background: var(--primary-hover);
        transform: translateY(-2px);
        box-shadow: none;
    }
    .about-section {
        background: var(--bg-secondary);
    }
    .about-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(500px, 1fr));
        gap: 4rem;
        align-items: center;
    }
    .about-content h3 {
        font-size: 2rem;
        color: var(--primary);
        margin-bottom: 1.5rem;
    }
    .about-content p {
        color: var(--text-secondary);
        margin-bottom: 1.5rem;
        font-size: 1.1rem;
    }
    .about-image {
        width: 100%;
        height: 400px;
        object-fit: cover;
        border-radius: 16px;
    }
    .features-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 2rem;
    }
    .feature-card {
        background: var(--bg-primary);
        padding: 2rem;
        border-radius: 16px;
        text-align: center;
        box-shadow: var(--shadow);
        transition: all 0.3s ease;
    }
    .feature-card:hover {
        transform: translateY(-5px);
    }
    .feature-icon {
        width: 80px;
        height: 80px;
        background: var(--gradient-primary);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 1.5rem;
        color: white;
        font-size: 2rem;
    }
    .testimonials-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
        gap: 2rem;
    }
    .testimonial-card {
        background: var(--bg-primary);
        padding: 2rem;
        border-radius: 16px;
        box-shadow: var(--shadow);
        position: relative;
    }
    .testimonial-card::before {
        content: '"';
        position: absolute;
        top: -10px;
        left: 20px;
        font-size: 4rem;
        color: var(--primary);
        opacity: 0.3;
    }
    .testimonial-text {
        font-style: italic;
        margin-bottom: 1.5rem;
        color: var(--text-secondary);
    }
    .testimonial-author {
        font-weight: 600;
        color: var(--primary);
    }
    .gallery-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 1rem;
    }
    .gallery-item {
        position: relative;
        overflow: hidden;
        border-radius: 12px;
        aspect-ratio: 4/3;
    }
    .gallery-image {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: transform 0.3s ease;
    }
    .gallery-item:hover .gallery-image {
        transform: scale(1.1);
    }
    .gallery-overlay {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: linear-gradient(to bottom, transparent, rgba(0,0,0,0.7));
        display: flex;
        align-items: flex-end;
        padding: 1.5rem;
        color: white;
        opacity: 0;
        transition: opacity 0.3s ease;
    }
    .gallery-item:hover .gallery-overlay {
        opacity: 1;
    }
    /* Modal Styles (Copied from products.ejs for consistency) */
    .modal {
        display: none;
        position: fixed;
        z-index: 2000;
        left: 0;
        top: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(0,0,0,0.5);
        backdrop-filter: blur(5px);
    }

    .modal-content {
        background-color: var(--bg-primary);
        margin: 2% auto;
        padding: 0;
        border-radius: 12px;
        width: 90%;
        max-width: 900px;
        box-shadow: var(--shadow-hover);
        position: relative;
        max-height: 90vh;
        overflow-y: auto;
    }

    .modal-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 1.5rem 2rem;
        border-bottom: 1px solid var(--border);
    }

    .modal-title {
        font-family: 'Poppins', sans-serif;
        font-size: 1.5rem;
        font-weight: 600;
        color: var(--text-primary);
    }

    .close {
        font-size: 1.5rem;
        cursor: pointer;
        color: var(--text-secondary);
        transition: var(--transition);
        width: 32px;
        height: 32px;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 50%;
    }

    .close:hover {
        color: var(--primary);
        background: var(--bg-secondary);
    }

    .modal-body {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 2rem;
        padding: 2rem;
    }

    .modal-image-section {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }

    .modal-main-image {
        width: 100%;
        height: 300px;
        object-fit: cover;
        border-radius: 8px;
        border: 1px solid var(--border);
    }

    .modal-thumbnails {
        display: flex;
        gap: 0.5rem;
        overflow-x: auto;
    }

    .modal-thumbnail {
        width: 60px;
        height: 60px;
        object-fit: cover;
        border-radius: 6px;
        cursor: pointer;
        border: 2px solid transparent;
        transition: var(--transition);
    }

    .modal-thumbnail:hover,
    .modal-thumbnail.active {
        border-color: var(--primary);
    }

    .modal-info-section {
        display: flex;
        flex-direction: column;
        gap: 1.5rem;
    }

    .modal-product-name {
        font-family: 'Poppins', sans-serif;
        font-size: 1.8rem;
        font-weight: 700;
        color: var(--text-primary);
        line-height: 1.3;
    }

    .modal-product-model {
        color: var(--text-secondary);
        font-size: 14px;
        font-weight: 500;
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }

    .modal-product-description {
        color: var(--text-secondary);
        line-height: 1.6;
    }

    .modal-features {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 0.5rem;
    }

    .modal-feature-item {
        display: flex;
        align-items: center;
        gap: 8px;
        color: var(--text-secondary);
        font-size: 14px;
    }

    .modal-feature-icon {
        color: var(--primary);
        font-size: 12px;
        width: 16px;
    }

    .modal-actions {
        display: flex;
        gap: 1rem;
        margin-top: 1rem;
    }

    .modal-btn {
        flex: 1;
        padding: 12px 20px;
        border: none;
        border-radius: 8px;
        font-weight: 500;
        cursor: pointer;
        transition: var(--transition);
        text-decoration: none;
        text-align: center;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 8px;
    }

    .modal-btn-primary {
        background: var(--primary);
        color: white;
    }

    .modal-btn-primary:hover {
        background: var(--primary-hover);
    }

    .modal-btn-secondary {
        background: #25D366;
        color: white;
    }

    .modal-btn-secondary:hover {
        background: #20b358;
    }
    /* End Modal Styles */

    .footer {
        background: var(--bg-dark);
        color: var(--text-light);
        padding: 4rem 2rem 2rem;
    }
    .footer-content {
        max-width: 1400px;
        margin: 0 auto;
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 3rem;
    }
    .footer-section h3 {
        color: var(--primary);
        margin-bottom: 1.5rem;
        font-size: 1.3rem;
    }
    .footer-section p,
    .footer-section a {
        color: #ccc;
        text-decoration: none;
        line-height: 1.8;
        transition: color 0.3s ease;
    }
    .footer-section a:hover {
        color: var(--primary);
    }
    .footer-bottom {
        text-align: center;
        padding-top: 2rem;
        border-top: 1px solid #333;
        color: #999;
        margin-top: 2rem;
    }
    @media (max-width: 1024px) {
        .modal-body {
            grid-template-columns: 1fr;
        }
    }
    @media (max-width: 768px) {
        .nav-menu {
            display: none;
            position: absolute;
            top: 100%;
            left: 0;
            width: 100%;
            background: var(--bg-primary);
            flex-direction: column;
            padding: 2rem;
            border-top: 1px solid var(--border);
            box-shadow: var(--shadow);
        }
        .nav-menu.active {
            display: flex;
        }
        .mobile-toggle {
            display: block;
        }
        .hero h1 {
            font-size: 2.5rem;
        }
        .hero p {
            font-size: 1.1rem;
        }
        .hero-buttons {
            flex-direction: column;
            align-items: center;
        }
        .categories-grid {
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1rem;
        }
        .products-grid {
            grid-template-columns: 1fr;
            gap: 1.5rem;
        }
        .about-grid {
            grid-template-columns: 1fr;
        }
        .product-actions {
            flex-direction: column;
        }
        .stats-grid {
            grid-template-columns: repeat(2, 1fr);
            gap: 2rem;
        }
        .section {
            padding: 3rem 1rem;
        }
        .category-content {
            padding: 1.25rem;
        }
        .product-info {
            padding: 1.5rem;
        }
        .modal-content {
            width: 95%;
            margin: 5% auto;
        }
        .modal-body {
            padding: 1.5rem;
        }
        .modal-actions {
            flex-direction: column;
        }
    }
    @media (max-width: 480px) {
        .nav-container {
            padding: 0 1rem;
        }
        .hero {
            padding: 6rem 1rem;
        }
        .stats-grid {
            grid-template-columns: 1fr;
        }
        .categories-grid {
            grid-template-columns: 1fr;
        }
    }
</style>
</head>
<body>
    <div class="preloader" id="preloader">
        <div class="loader"></div>
    </div>
    <nav class="navbar" id="navbar">
        <div class="nav-container">
            <a href="/" class="logo-container">
                <div class="logo-icon">
                    <i class="fas fa-industry"></i>
                </div>
                <span>Aspire Pack Automation</span>
            </a>
            
            <ul class="nav-menu" id="navMenu">
                <li><a href="/" class="nav-link">Home</a></li>
                <li><a href="/products" class="nav-link">Products</a></li>
                <li><a href="/about" class="nav-link">About</a></li>
                <li><a href="/services" class="nav-link">Services & Support</a></li>
                <li><a href="/news" class="nav-link">News</a></li>
                <li><a href="/contact" class="nav-link">Contact</a></li>
            </ul>
            <div class="nav-actions">
                <button class="theme-toggle" id="themeToggle">
                    <i class="fas fa-moon" id="themeIcon"></i>
                </button>
                <% if (typeof user !== 'undefined' && user) { %>
                    <% if (user.role === 'admin') { %>
                        <a href="/admin/dashboard" class="btn-primary">Admin Panel</a>
                    <% } %>
                    <a href="/logout" class="btn-primary">Logout</a>
                <% } else { %>
                    <a href="/login" class="btn-primary">Login</a>
                <% } %>
                <button class="mobile-toggle" id="mobileToggle">
                    <i class="fas fa-bars"></i>
                </button>
            </div>
        </div>
    </nav>
    <section class="hero" data-aos="fade-in">
        <div class="hero-content">
            <h1 data-aos="fade-up" data-aos-delay="200">Advanced Packaging Machinery Solutions</h1>
            <p data-aos="fade-up" data-aos-delay="400">Powering industries with cutting-edge automation, uncompromising quality, and innovative technology in every seal, wrap, and package.</p>
            <div class="hero-buttons" data-aos="fade-up" data-aos-delay="600">
                <a href="/products" class="btn-primary btn-hero">Explore Products</a>
                <a href="/contact" class="btn-primary btn-hero secondary">Get Quote</a>
            </div>
        </div>
    </section>
    <section class="stats" data-aos="fade-up">
        <div class="stats-container">
            <div class="stats-grid">
                <div class="stat-item" data-aos="fade-up" data-aos-delay="100">
                    <div class="stat-number">10+</div>
                    <div class="stat-label">Product Categories</div>
                </div>
                <div class="stat-item" data-aos="fade-up" data-aos-delay="200">
                    <div class="stat-number"><%= (typeof totalProducts !== 'undefined' && totalProducts) ? totalProducts : '100' %>+</div>
                    <div class="stat-label">Packaging Machines</div>
                </div>
                <div class="stat-item" data-aos="fade-up" data-aos-delay="300">
                    <div class="stat-number">200+</div>
                    <div class="stat-label">Happy Clients</div>
                </div>
                <div class="stat-item" data-aos="fade-up" data-aos-delay="400">
                    <div class="stat-number">5+</div>
                    <div class="stat-label">Years Experience</div>
                </div>
            </div>
        </div>
    </section>
    <section class="section">
        <div class="container">
            <div class="section-header" data-aos="fade-up">
                <h2 class="section-title">Our Product Categories</h2>
                <p class="section-subtitle">Discover our comprehensive range of industrial packaging machinery across multiple specialized categories.</p>
            </div>
            
            <div class="categories-grid">
                <% 
                let categoriesToShow = [];
                if (typeof categories !== 'undefined' && categories && Array.isArray(categories) && categories.length > 0) {
                    categoriesToShow = categories;
                } else {
                    categoriesToShow = [
                        {
                            name: 'Vacuum Sealing Machines',
                            description: 'Professional vacuum sealing equipment for food preservation and industrial applications.',
                            productCount: 25,
                            coverImage: 'https://images.unsplash.com/photo-1581091226825-a6a2a5aee158?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80',
                            slug: 'vacuum-sealing'
                        },
                        {
                            name: 'Box Packing Machines',
                            description: 'Automated box packing solutions for efficient product packaging operations.',
                            productCount: 18,
                            coverImage: 'https://images.unsplash.com/photo-1565793298595-6a879b1d9492?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80',
                            slug: 'box-packing'
                        },
                        {
                            name: 'Wrapping Machines',
                            description: 'Advanced wrapping systems for pallets and industrial packaging.',
                            productCount: 22,
                            coverImage: 'https://images.unsplash.com/photo-1586528116311-ad8dd3c8310d?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80',
                            slug: 'wrapping-machines'
                        },
                        {
                            name: 'Filling Machines',
                            description: 'Precision filling equipment for liquids and granular materials.',
                            productCount: 30,
                            coverImage: 'https://images.unsplash.com/photo-1581093588401-5c89cbd8995c?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80',
                            slug: 'filling-machines'
                        },
                        {
                            name: 'Labeling Systems',
                            description: 'Automated labeling solutions for bottles and containers.',
                            productCount: 15,
                            coverImage: 'https://images.unsplash.com/photo-1586953208448-b95a79798f07?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80',
                            slug: 'labeling-systems'
                        },
                        {
                            name: 'Conveyor Systems',
                            description: 'Industrial conveyor belts for production lines.',
                            productCount: 12,
                            coverImage: 'https://images.unsplash.com/photo-1581092160562-40aa08e78837?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80',
                            slug: 'conveyor-systems'
                        }
                    ];
                }
                %>
                
                <% categoriesToShow.forEach((category, index) => { %>
                    <div class="category-card" data-aos="fade-up" data-aos-delay="<%= index * 100 %>">
                        <img src="<%= category.coverImage || '/placeholder.svg?height=180&width=280' %>" 
                             alt="<%= category.name || category.categoryName || 'Category' %>" class="category-image">
                        <div class="category-content">
                            <h3 class="category-title"><%= category.name || category.categoryName || 'Category' %></h3>
                            <p class="category-description"><%= category.description || 'Discover high-quality packaging machines for various industrial needs.' %></p>
                            <div class="category-meta">
                                <span class="product-count"><%= category.productCount || Math.floor(Math.random() * 20) + 10 %> Products</span>
                                <span class="category-arrow"><i class="fas fa-arrow-right"></i></span>
                            </div>
                            <a href="/category/<%= category.slug || category._id || 'category' %>" class="category-btn">
                                Explore Category
                            </a>
                        </div>
                    </div>
                <% }) %>
            </div>
            <div style="text-align: center; margin-top: 3rem;" data-aos="fade-up">
                <a href="/products" class="btn-primary" style="padding: 1rem 2rem; font-size: 1.1rem;">View All Categories</a>
            </div>
        </div>
    </section>
    <section class="section" style="background: var(--bg-secondary);">
        <div class="container">
            <div class="section-header" data-aos="fade-up">
                <h2 class="section-title">Featured Products</h2>
                <p class="section-subtitle">Discover our most popular and innovative packaging machines trusted by industry leaders worldwide.</p>
            </div>
            
            <div class="products-grid">
                <% 
                let productsToShow = [];
                if (typeof topProducts !== 'undefined' && topProducts && Array.isArray(topProducts) && topProducts.length > 0) {
                    productsToShow = topProducts.slice(0, 6);
                } else if (typeof featuredProducts !== 'undefined' && featuredProducts && Array.isArray(featuredProducts) && featuredProducts.length > 0) {
                    productsToShow = featuredProducts.slice(0, 6);
                } else if (typeof products !== 'undefined' && products && Array.isArray(products) && products.length > 0) {
                    productsToShow = products.slice(0, 6);
                } else {
                    productsToShow = [
                        {
                            productName: 'Professional Vacuum Sealer VS-2000',
                            productModelNumber: 'VS-2000-PRO',
                            coverImage: 'https://images.unsplash.com/photo-1581091226825-a6a2a5aee158?ixlib=rb-4.0.3&auto=format&fit=crop&w=600&q=80',
                            slug: 'vacuum-sealer-vs-2000',
                            badge: 'Best Seller',
                            description: 'High-quality packaging machinery designed for efficiency and reliability. Perfect for industrial applications with advanced features and robust construction.',
                            features: ['High Efficiency', 'Advanced Technology', 'Quality Assured']
                        },
                        {
                            productName: 'Automatic Box Packing Machine',
                            productModelNumber: 'ABP-3000',
                            coverImage: 'https://images.unsplash.com/photo-1565793298595-6a879b1d9492?ixlib=rb-4.0.3&auto=format&fit=crop&w=600&q=80',
                            slug: 'box-packing-abp-3000',
                            badge: 'Featured',
                            description: 'Automated box packing solutions for efficient product packaging operations.',
                            features: ['High Efficiency', 'Advanced Technology', 'Quality Assured']
                        },
                        {
                            productName: 'Industrial Wrapping System',
                            productModelNumber: 'IWS-1500',
                            coverImage: 'https://images.unsplash.com/photo-1586528116311-ad8dd3c8310d?ixlib=rb-4.0.3&auto=format&fit=crop&w=600&q=80',
                            slug: 'wrapping-system-iws-1500',
                            badge: 'Premium',
                            description: 'Advanced wrapping systems for pallets and industrial packaging.',
                            features: ['High Efficiency', 'Advanced Technology', 'Quality Assured']
                        },
                        {
                            productName: 'Precision Filling Machine',
                            productModelNumber: 'PFM-500',
                            coverImage: 'https://images.unsplash.com/photo-1581093588401-5c89cbd8995c?ixlib=rb-4.0.3&auto=format&fit=crop&w=600&q=80',
                            slug: 'filling-machine-pfm-500',
                            badge: 'Popular',
                            description: 'Precision filling equipment for liquids and granular materials.',
                            features: ['High Efficiency', 'Advanced Technology', 'Quality Assured']
                        },
                        {
                            productName: 'Automated Labeling System',
                            productModelNumber: 'ALS-800',
                            coverImage: 'https://images.unsplash.com/photo-1586953208448-b95a79798f07?ixlib=rb-4.0.3&auto=format&fit=crop&w=600&q=80',
                            slug: 'labeling-system-als-800',
                            badge: 'New',
                            description: 'Automated labeling solutions for bottles and containers.',
                            features: ['High Efficiency', 'Advanced Technology', 'Quality Assured']
                        },
                        {
                            productName: 'Heavy Duty Conveyor Belt',
                            productModelNumber: 'HDCB-1200',
                            coverImage: 'https://images.unsplash.com/photo-1581092160562-40aa08e78837?ixlib=rb-4.0.3&auto=format&fit=crop&w=600&q=80',
                            slug: 'conveyor-belt-hdcb-1200',
                            badge: 'Industrial',
                            description: 'Industrial conveyor belts for production lines.',
                            features: ['High Efficiency', 'Advanced Technology', 'Quality Assured']
                        }
                    ];
                }
                %>
                
                <% productsToShow.forEach((prod, index) => { %>
                    <div class="product-card" 
                         data-aos="fade-up" 
                         data-aos-delay="<%= index * 100 %>"
                         onclick="location.href='/product/<%= prod.slug %>'">
                        
                        <div class="product-image-container">
                            <img src="<%= prod.coverImage || '/placeholder.svg?height=220&width=280' %>" 
                                 alt="<%= prod.productName %>" 
                                 class="product-image"
                                 loading="lazy">
                            <div class="product-badge"><%= prod.badge || 'Featured' %></div>
                            <button class="quick-view-btn" onclick="event.stopPropagation(); openQuickView('<%= prod._id || 'product-id-' + index %>', '<%= prod.productName %>', '<%= prod.productModelNumber || prod.modelNumber || '' %>', '<%= prod.coverImage || '' %>', '<%= (prod.shortDescription || 'High-quality packaging machinery designed for efficiency and reliability. Perfect for industrial applications with advanced features and robust construction.').replace(/'/g, "\\'") %>');">
                                <i class="fas fa-eye"></i>
                            </button>
                        </div>
                        
                        <div class="product-info">
                            <h4 class="product-name"><%= prod.productName %></h4>
                            <% if (prod.productModelNumber || prod.modelNumber) { %>
                                <p class="product-model">Model: <%= prod.productModelNumber || prod.modelNumber %></p>
                            <% } %>
                            
                            <div class="product-features">
                                <% (prod.features || ['High Efficiency', 'Advanced Technology', 'Quality Assured']).forEach(feature => { %>
                                    <div class="feature-item">
                                        <i class="fas fa-check-circle feature-icon-product"></i>
                                        <span><%= feature %></span>
                                    </div>
                                <% }) %>
                            </div>
                            
                            <button class="inquire-btn" onclick="event.stopPropagation(); openInquiryModal('<%= prod.productName %>', '<%= prod.productModelNumber || prod.modelNumber || '' %>');">
                                <i class="fas fa-envelope"></i>
                                Inquire Now
                            </button>
                        </div>
                    </div>
                <% }) %>
            </div>
            <div style="text-align: center; margin-top: 3rem;" data-aos="fade-up">
                <a href="/products" class="btn-primary" style="padding: 1rem 2rem; font-size: 1.1rem;">View All Products</a>
            </div>
        </div>
    </section>
    <section class="section">
        <div class="container">
            <div class="section-header" data-aos="fade-up">
                <h2 class="section-title">Why Choose Aspire Packaging</h2>
                <p class="section-subtitle">Industry-leading features that set our packaging machinery apart from the competition.</p>
            </div>
            
            <div class="features-grid">
                <div class="feature-card" data-aos="fade-up" data-aos-delay="100">
                    <div class="feature-icon">
                        <i class="fas fa-cogs"></i>
                    </div>
                    <h3>Advanced Automation</h3>
                    <p>State-of-the-art automated systems that increase efficiency and reduce manual labor costs.</p>
                </div>
                
                <div class="feature-card" data-aos="fade-up" data-aos-delay="200">
                    <div class="feature-icon">
                        <i class="fas fa-shield-alt"></i>
                    </div>
                    <h3>Quality Assurance</h3>
                    <p>ISO certified manufacturing processes ensuring consistent quality and reliability in every machine.</p>
                </div>
                
                <div class="feature-card" data-aos="fade-up" data-aos-delay="300">
                    <div class="feature-icon">
                        <i class="fas fa-tools"></i>
                    </div>
                    <h3>24/7 Support</h3>
                    <p>Round-the-clock technical support and maintenance services to keep your operations running smoothly.</p>
                </div>
                
                <!-- <div class="feature-card" data-aos="fade-up" data-aos-delay="400">
                    <div class="feature-icon">
                        <i class="fas fa-leaf"></i>
                    </div>
                    <h3>Eco-Friendly</h3>
                    <p>Energy-efficient designs that reduce environmental impact while maintaining peak performance.</p>
                </div> -->
                
                <div class="feature-card" data-aos="fade-up" data-aos-delay="500">
                    <div class="feature-icon">
                        <i class="fas fa-chart-line"></i>
                    </div>
                    <h3>Scalable Solutions</h3>
                    <p>Flexible machinery that grows with your business needs and production requirements.</p>
                </div>
                
                <!-- <div class="feature-card" data-aos="fade-up" data-aos-delay="600">
                    <div class="feature-icon">
                        <i class="fas fa-award"></i>
                    </div>
                    <h3>Industry Recognition</h3>
                    <p>Award-winning designs recognized by industry leaders and packaging professionals worldwide.</p>
                </div> -->
            </div>
        </div>
    </section>
    <section class="section about-section">
        <div class="container">
            <div class="about-grid">
                <div class="about-content" data-aos="fade-right">
                    <h3>About Aspire Packaging</h3>
                    <p>With over 15 years of excellence in the packaging machinery industry, Aspire Packaging has established itself as a trusted partner for businesses seeking reliable, innovative packaging solutions.</p>
                    <p>Our commitment to quality, innovation, and customer satisfaction has made us a preferred choice for companies across various industries including food & beverage, pharmaceuticals, cosmetics, and industrial manufacturing.</p>
                    <p>From small-scale operations to large industrial facilities, we provide customized packaging machinery that meets the unique requirements of each client while maintaining the highest standards of quality and performance.</p>
                    <a href="/about" class="btn-primary">Learn More About Us</a>
                </div>
                <div data-aos="fade-left">
                    <img src="https://images.unsplash.com/photo-1581092160562-40aa08e78837?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80" alt="Manufacturing Facility" class="about-image">
                </div>
            </div>
        </div>
    </section>
    <section class="section">
        <div class="container">
            <div class="section-header" data-aos="fade-up">
                <h2 class="section-title">Our Manufacturing Excellence</h2>
                <p class="section-subtitle">Take a glimpse into our state-of-the-art manufacturing facilities and quality control processes.</p>
            </div>
            
            <div class="gallery-grid">
                <div class="gallery-item" data-aos="fade-up" data-aos-delay="100">
                    <img src="https://images.unsplash.com/photo-1581092160562-40aa08e78837?ixlib=rb-4.0.3&auto=format&fit=crop&w=600&q=80" alt="Manufacturing Floor" class="gallery-image">
                    <div class="gallery-overlay">
                        <h4>Testing Facility</h4>
                    </div>
                </div>
                
                <div class="gallery-item" data-aos="fade-up" data-aos-delay="200">
                    <img src="https://images.unsplash.com/photo-1565793298595-6a879b1d9492?ixlib=rb-4.0.3&auto=format&fit=crop&w=600&q=80" alt="Quality Control" class="gallery-image">
                    <div class="gallery-overlay">
                        <h4>Quality Control Lab</h4>
                    </div>
                </div>
                
                <div class="gallery-item" data-aos="fade-up" data-aos-delay="300">
                    <img src="https://images.unsplash.com/photo-1581091226825-a6a2a5aee158?ixlib=rb-4.0.3&auto=format&fit=crop&w=600&q=80" alt="Assembly Line" class="gallery-image">
                    <div class="gallery-overlay">
                        <h4>Assembly Line</h4>
                    </div>
                </div>
                
                <div class="gallery-item" data-aos="fade-up" data-aos-delay="400">
                    <img src="https://images.unsplash.com/photo-1586528116311-ad8dd3c8310d?ixlib=rb-4.0.3&auto=format&fit=crop&w=600&q=80" alt="Testing Facility" class="gallery-image">
                    <div class="gallery-overlay">
                        <h4>Warehouse & Logistics</h4>
                    </div>
                  </div>
                
                
            </div>
        </div>
    </section>
    <section class="section" style="background: var(--bg-secondary);">
        <div class="container">
            <div class="section-header" data-aos="fade-up">
                <h2 class="section-title">What Our Clients Say</h2>
                <p class="section-subtitle">Hear from industry leaders who trust Aspire Packaging for their critical packaging operations.</p>
            </div>
            
            <div class="testimonials-grid">
                <div class="testimonial-card" data-aos="fade-up" data-aos-delay="100">
                    <p class="testimonial-text">The vacuum sealing machines from Aspire Packaging have revolutionized our food preservation process. The quality and reliability are unmatched, and their support team is exceptional.</p>
                    <div class="testimonial-author">— Sarah Johnson, Production Manager at Fresh Foods Inc.</div>
                </div>
                
                <div class="testimonial-card" data-aos="fade-up" data-aos-delay="200">
                    <p class="testimonial-text">We've been using Aspire's box packing machines for over 3 years now. The efficiency gains and reduced labor costs have significantly improved our bottom line. Highly recommended!</p>
                    <div class="testimonial-author">— Michael Chen, Operations Director at PackTech Solutions</div>
                </div>
                
                <div class="testimonial-card" data-aos="fade-up" data-aos-delay="300">
                    <p class="testimonial-text">The automated wrapping system has streamlined our entire packaging line. The ROI was achieved within 8 months, and the machine continues to perform flawlessly.</p>
                    <div class="testimonial-author">— David Rodriguez, Plant Manager at Industrial Packaging Co.</div>
                </div>
                
                <div class="testimonial-card" data-aos="fade-up" data-aos-delay="400">
                    <p class="testimonial-text">Aspire Packaging's filling machines have helped us maintain consistent quality while increasing our production capacity by 40%. Their technical expertise is outstanding.</p>
                    <div class="testimonial-author">— Lisa Thompson, Quality Assurance Lead at BevCorp</div>
                </div>
                
                <div class="testimonial-card" data-aos="fade-up" data-aos-delay="500">
                    <p class="testimonial-text">The labeling system integration was seamless, and the precision is remarkable. Our product presentation has improved significantly, leading to better market reception.</p>
                    <div class="testimonial-author">— Robert Kim, Brand Manager at Premium Cosmetics</div>
                </div>
                
                <div class="testimonial-card" data-aos="fade-up" data-aos-delay="600">
                    <p class="testimonial-text">From initial consultation to installation and ongoing support, Aspire Packaging has been a true partner in our growth. Their machines are built to last and perform.</p>
                    <div class="testimonial-author">— Amanda Foster, CEO at GreenTech Manufacturing</div>
                </div>
            </div>
        </div>
    </section>

    <!-- Quick View Modal (Copied from products.ejs) -->
    <div id="quickViewModal" class="modal">
        <div class="modal-content">
            <div class="modal-header">
                <h2 class="modal-title" id="modalTitle">Product Quick View</h2>
                <span class="close" id="closeQuickView">&times;</span>
            </div>
            <div class="modal-body">
                <div class="modal-image-section">
                    <img id="modalMainImage" class="modal-main-image" src="/placeholder.svg" alt="">
                    <div class="modal-thumbnails" id="modalThumbnails">
                        <!-- Thumbnails will be dynamically added here -->
                    </div>
                </div>
                <div class="modal-info-section">
                    <h3 class="modal-product-name" id="modalProductName"></h3>
                    <p class="modal-product-model" id="modalProductModel"></p>
                    <p class="modal-product-description" id="modalProductDescription"></p>
                    
                    <div class="modal-features">
                        <div class="modal-feature-item">
                            <i class="fas fa-check-circle modal-feature-icon"></i>
                            <span>High Efficiency Performance</span>
                        </div>
                        <div class="modal-feature-item">
                            <i class="fas fa-cog modal-feature-icon"></i>
                            <span>Advanced Technology</span>
                        </div>
                        <div class="modal-feature-item">
                            <i class="fas fa-shield-alt modal-feature-icon"></i>
                            <span>Quality Assured</span>
                        </div>
                        <div class="modal-feature-item">
                            <i class="fas fa-tools modal-feature-icon"></i>
                            <span>Easy Maintenance</span>
                        </div>
                        <div class="modal-feature-item">
                            <i class="fas fa-award modal-feature-icon"></i>
                            <span>ISO Certified</span>
                        </div>
                        <div class="modal-feature-item">
                            <i class="fas fa-headset modal-feature-icon"></i>
                            <span>24/7 Support</span>
                        </div>
                    </div>
                    
                    <div class="modal-actions">
                        <a id="modalWhatsappBtn" class="modal-btn modal-btn-secondary" target="_blank">
                            <i class="fab fa-whatsapp"></i> WhatsApp
                        </a>
                        <a id="modalEmailBtn" class="modal-btn modal-btn-primary">
                            <i class="fas fa-envelope"></i> Email Inquiry
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Inquiry Modal (Renamed from contactModal and updated) -->
    <div id="inquiryModal" class="modal">
        <div class="modal-content" style="max-width: 500px;">
            <div class="modal-header">
                <h2 class="modal-title">Inquiry Options</h2>
                <span class="close" id="closeInquiry">&times;</span>
            </div>
            <div class="modal-body" style="display: block; padding: 2rem;">
                <p style="margin-bottom: 2rem; color: var(--text-secondary);">Choose your preferred method to inquire about <strong><span id="inquiryProductName"></span></strong></p>
                
                <div style="display: flex; gap: 1rem; flex-wrap: wrap;">
                    <a id="inquiryWhatsappLink" class="modal-btn modal-btn-secondary" style="flex: 1; min-width: 120px;">
                        <i class="fab fa-whatsapp"></i> WhatsApp
                    </a>
                    <a id="inquiryEmailLink" class="modal-btn modal-btn-primary" style="flex: 1; min-width: 120px;">
                        <i class="fas fa-envelope"></i> Email
                    </a>
                    
                </div>
            </div>
        </div>
    </div>

    <footer class="footer">
        <div class="footer-content">
            <div class="footer-section">
                <h3>Aspire Packaging</h3>
                <p>Leading manufacturer and supplier of industrial packaging machinery with over 15 years of industry experience and innovation.</p>
                <p>Trusted by thousands of businesses worldwide for reliable, efficient, and cutting-edge packaging solutions.</p>
            </div>
            
            <div class="footer-section">
                <h3>Quick Links</h3>
                <p><a href="/">Home</a></p>
                <p><a href="/products">Products</a></p>
                <p><a href="/categories">Categories</a></p>
                <p><a href="/services">Services & Support</a></p>
                <p><a href="/about">About Us</a></p>
                <p><a href="/contact">Contact</a></p>
            </div>
            
            <div class="footer-section">
                <h3>Product Categories</h3>
                <p><a href="/category/vacuum-sealing">Vacuum Sealing</a></p>
                <p><a href="/category/box-packing">Box Packing</a></p>
                <p><a href="/category/wrapping-machines">Wrapping Machines</a></p>
                <p><a href="/category/filling-machines">Filling Machines</a></p>
                <p><a href="/category/labeling-systems">Labeling Systems</a></p>
                <p><a href="/category/conveyor-systems">Conveyor Systems</a></p>
            </div>
            
            <div class="footer-section">
                <h3>Contact Information</h3>
                <p><i class="fas fa-phone"></i> +91 7424860111</p>
                <p><i class="fas fa-envelope"></i> aspirepackautomation@gmail.com</p>
                <p><i class="fas fa-envelope"></i> sales@aspirepackaging.com</p>
                <p><i class="fas fa-map-marker-alt"></i> Industrial Area, Sector 62, Noida, UP, India</p>
                <p><i class="fas fa-clock"></i> Mon-Sat: 9:00 AM - 6:00 PM</p>
            </div>
        </div>
        
        <div class="footer-bottom">
            <p>&copy; <%= new Date().getFullYear() %> Aspire Packaging. All rights reserved. | Engineered for Excellence | ISO 9001:2015 Certified</p>
        </div>
    </footer>
    <script>
        window.addEventListener('load', function() {
            const preloader = document.getElementById('preloader');
            setTimeout(() => {
                preloader.classList.add('hidden');
            }, 1000);
        });
        AOS.init({
            duration: 1000,
            once: true,
            offset: 100
        });
        const themeToggle = document.getElementById('themeToggle');
        const themeIcon = document.getElementById('themeIcon');
        const body = document.body;

        const currentTheme = localStorage.getItem('theme') || 'light';
        body.setAttribute('data-theme', currentTheme);
        themeIcon.className = currentTheme === 'dark' ? 'fas fa-sun' : 'fas fa-moon';
        themeToggle.addEventListener('click', function() {
            const currentTheme = body.getAttribute('data-theme');
            const newTheme = currentTheme === 'dark' ? 'light' : 'dark';
            
            body.setAttribute('data-theme', newTheme);
            localStorage.setItem('theme', newTheme);
            themeIcon.className = newTheme === 'dark' ? 'fas fa-sun' : 'fas fa-moon';
        });

        const mobileToggle = document.getElementById('mobileToggle');
        const navMenu = document.getElementById('navMenu');
        mobileToggle.addEventListener('click', function() {
            navMenu.classList.toggle('active');
            const icon = this.querySelector('i');
            icon.className = navMenu.classList.contains('active') ? 'fas fa-times' : 'fas fa-bars';
            document.body.style.overflow = navMenu.classList.contains('active') ? 'hidden' : 'auto';
        });

        navMenu.querySelectorAll('.nav-link, .btn-primary, .theme-toggle').forEach(link => {
            link.addEventListener('click', () => {
                if (navMenu.classList.contains('active')) {
                    navMenu.classList.remove('active');
                    mobileToggle.querySelector('i').className = 'fas fa-bars';
                    document.body.style.overflow = 'auto';
                }
            });
        });

        const navbar = document.getElementById('navbar');
        window.addEventListener('scroll', function() {
            if (window.scrollY > 100) {
                navbar.classList.add('scrolled');
            } else {
                navbar.classList.remove('scrolled');
            }
        });

        // Quick View Modal Logic (Copied and adapted from products.ejs)
        const quickViewModal = document.getElementById('quickViewModal');
        const closeQuickView = document.getElementById('closeQuickView');

        function openQuickView(productId, productName, modelNumber, image, description) {
            document.getElementById('modalTitle').textContent = 'Product Quick View';
            document.getElementById('modalProductName').textContent = productName;
            document.getElementById('modalProductModel').textContent = modelNumber ? `Model: ${modelNumber}` : '';
            document.getElementById('modalProductDescription').textContent = description || 'High-quality packaging machinery designed for efficiency and reliability. Perfect for industrial applications with advanced features and robust construction.';
            document.getElementById('modalMainImage').src = image || '/placeholder.svg?height=300&width=400';
            document.getElementById('modalMainImage').alt = productName;
            
            // Clear existing thumbnails (if any)
            const modalThumbnails = document.getElementById('modalThumbnails');
            modalThumbnails.innerHTML = '';

            // Add main image as a thumbnail (for consistency, if multiple images were available)
            const mainThumbnail = document.createElement('img');
            mainThumbnail.src = image || '/placeholder.svg?height=60&width=60';
            mainThumbnail.alt = productName;
            mainThumbnail.classList.add('modal-thumbnail', 'active');
            mainThumbnail.onclick = () => {
                document.getElementById('modalMainImage').src = mainThumbnail.src;
                document.querySelectorAll('.modal-thumbnail').forEach(thumb => thumb.classList.remove('active'));
                mainThumbnail.classList.add('active');
            };
            modalThumbnails.appendChild(mainThumbnail);

            // You would add more thumbnails here if prod.images array was available
            // For example:
            // if (prod.images && prod.images.length > 0) {
            //     prod.images.forEach(imgUrl => {
            //         const thumb = document.createElement('img');
            //         thumb.src = imgUrl;
            //         thumb.alt = productName;
            //         thumb.classList.add('modal-thumbnail');
            //         thumb.onclick = () => {
            //             document.getElementById('modalMainImage').src = thumb.src;
            //             document.querySelectorAll('.modal-thumbnail').forEach(t => t.classList.remove('active'));
            //             thumb.classList.add('active');
            //         };
            //         modalThumbnails.appendChild(thumb);
            //     });
            // }

            const whatsappText = `Hello! I'm interested in the ${productName}${modelNumber ? ` (Model: ${modelNumber})` : ''}. Could you please provide more details?`;
            const emailSubject = `Inquiry about ${productName}`;
            const emailBody = `Hello,\n\nI'm interested in learning more about the ${productName}${modelNumber ? ` (Model: ${modelNumber})` : ''}.\n\nPlease provide details about specifications, pricing, and availability.\n\nThank you!`;
            
            document.getElementById('modalWhatsappBtn').href = `https://wa.me/917424860111?text=${encodeURIComponent(whatsappText)}`;
            document.getElementById('modalEmailBtn').href = `mailto:aspirepackautomation@gmail.com?subject=${encodeURIComponent(emailSubject)}&body=${encodeURIComponent(emailBody)}`;
            
            quickViewModal.style.display = 'block';
            document.body.style.overflow = 'hidden';
        }

        closeQuickView.addEventListener('click', () => {
            quickViewModal.style.display = 'none';
            document.body.style.overflow = 'auto';
        });

        // Inquiry Modal Logic (Renamed and updated from contactModal)
        const inquiryModal = document.getElementById('inquiryModal');
        const closeInquiry = document.getElementById('closeInquiry');

        function openInquiryModal(productName, modelNumber) {
            document.getElementById('inquiryProductName').textContent = productName;
            
            const whatsappText = `Hello! I'm interested in the ${productName}${modelNumber ? ` (Model: ${modelNumber})` : ''}. Could you please provide more details?`;
            const emailSubject = `Inquiry about ${productName}`;
            const emailBody = `Hello,\n\nI'm interested in learning more about the ${productName}${modelNumber ? ` (Model: ${modelNumber})` : ''}.\n\nPlease provide details about specifications, pricing, and availability.\n\nThank you!`;
            
            document.getElementById('inquiryWhatsappLink').href = `https://wa.me/917424860111?text=${encodeURIComponent(whatsappText)}`;
            document.getElementById('inquiryEmailLink').href = `mailto:shivamjitendraverma@gmail.com?subject=${encodeURIComponent(emailSubject)}&body=${encodeURIComponent(emailBody)}`;
            
            inquiryModal.style.display = 'block';
            document.body.style.overflow = 'hidden';
        }

        closeInquiry.addEventListener('click', () => {
            inquiryModal.style.display = 'none';
            document.body.style.overflow = 'auto';
        });

        // Close modals when clicking outside or pressing Escape (Copied from products.ejs)
        window.addEventListener('click', (e) => {
            if (e.target === quickViewModal) {
                quickViewModal.style.display = 'none';
                document.body.style.overflow = 'auto';
            }
            if (e.target === inquiryModal) {
                inquiryModal.style.display = 'none';
                document.body.style.overflow = 'auto';
            }
        });

        document.addEventListener('keydown', function(e) {
            if (e.key === 'Escape') {
                if (quickViewModal.style.display === 'block') {
                    quickViewModal.style.display = 'none';
                    document.body.style.overflow = 'auto';
                }
                if (inquiryModal.style.display === 'block') {
                    inquiryModal.style.display = 'none';
                    document.body.style.overflow = 'auto';
                }
                // Close mobile menu if open
                if (navMenu.classList.contains('active')) {
                    navMenu.classList.remove('active');
                    mobileToggle.querySelector('i').className = 'fas fa-bars';
                    document.body.style.overflow = 'auto';
                }
            }
            
            if ((e.ctrlKey || e.metaKey) && e.key === 'k') {
                e.preventDefault();
                // Assuming you might have a search input on the homepage too
                // const searchInput = document.getElementById('searchInput');
                // if (searchInput) searchInput.focus();
            }
        });

        document.querySelectorAll('.btn-primary, .category-btn, .inquire-btn').forEach(button => {
            button.addEventListener('click', function() {
                if (!this.classList.contains('inquire-btn')) {
                    this.style.opacity = '0.8';
                    setTimeout(() => {
                        this.style.opacity = '1';
                    }, 200);
                }
            });
        });
        console.log('Aspire Packaging - Advanced packaging machinery solutions loaded successfully');
    </script>
</body>
</html>
